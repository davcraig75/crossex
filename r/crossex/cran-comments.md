## R CMD check results

0 errors | 0 warnings | 0 notes (run locally with `R CMD check --as-cran`)

* This is a new release.
* The package bundles the MIT-licensed crossex JavaScript library
  (built from the same repository, https://github.com/davcraig75/crossex),
  which itself bundles the BSD-3-licensed Vega runtime.
* All rendering happens client side; no network access is required or used.
