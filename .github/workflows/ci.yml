name: Verify

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm --prefix electron ci --ignore-scripts
      - run: npx playwright install --with-deps chromium
      - run: npm run verify
      - name: Ensure generated distributions were committed
        run: git diff --exit-code
      - run: npm audit --omit=dev
      - run: npm --prefix electron audit
