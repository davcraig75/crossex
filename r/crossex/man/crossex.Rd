% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossex.R
\name{crossex}
\alias{crossex}
\title{Interactive data exploration widget}
\usage{
crossex(
  data,
  x = NULL,
  y = NULL,
  color = NULL,
  size = NULL,
  facet_rows = NULL,
  facet_cols = NULL,
  editable = TRUE,
  exportable = TRUE,
  hide_panel = FALSE,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame. Factors are converted to character; `Date` and
`POSIXt` columns are converted to decimal years (e.g. `2023.4986`) so
they plot on a continuous axis.}

\item{x, y}{Column names for the initial X and Y axes. Defaults to the
first and second columns. Use `"None"` for a one-variable histogram.}

\item{color, size}{Optional column names for the color and point-size
encodings; default `"None"`.}

\item{facet_rows, facet_cols}{Optional column names that split the chart
into a grid of subplots; default `"None"`.}

\item{editable}{Show the control panel (tabs on the left). Default `TRUE`.}

\item{exportable}{Enable PNG/SVG/CSV export actions. Default `TRUE`.}

\item{hide_panel}{Hide the control panel entirely, for locked-down
dashboard views. Default `FALSE`.}

\item{width, height}{Widget size, passed to
[htmlwidgets::createWidget()]. Defaults fill the container.}

\item{elementId}{Optional explicit DOM id for the widget element.}
}
\value{
An object of class `htmlwidget` that prints itself in the RStudio
  viewer or browser, and renders inside Shiny apps and R Markdown documents.
}
\description{
Renders a full exploration environment for a data frame: configurable
charts, faceting, filtering, formula transforms, summary statistics, a data
table, and a 3D unit view, with PNG/SVG/CSV export.
}
\examples{
crossex(iris)
crossex(iris, x = "Sepal.Length", y = "Sepal.Width", color = "Species")
crossex(mtcars, x = "wt", y = "mpg", facet_cols = "cyl")

}
\seealso{
[crossexOutput()] and [renderCrossex()] for Shiny.
}
