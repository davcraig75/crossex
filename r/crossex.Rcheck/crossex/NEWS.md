# crossex 1.0.0

* First release: `crossex()` renders the crossex interactive data explorer
  (configurable charts, faceting, filtering, formula transforms, summary
  statistics, data table, 3D unit view, PNG/SVG/CSV export) for any data
  frame, with `crossexOutput()`/`renderCrossex()` for Shiny.
